let players = ["Joana", "Pierrick", "Amélie", "Manu"];
players[0];
players[1];
players[2];
players[3];
players[-1];
players[4];
players.length;
players[players.length-1];

let scores = [];
scores.push(2);
scores[0];
scores.pop();
scores.length;
